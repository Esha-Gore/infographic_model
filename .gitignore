__pycache__/
*.pyc
.DS_Store

# Pipeline outputs / intermediates (generated by run_pipeline.py)
pipeline_out/
pipeline_test/
embeddings_og.csv
embeddings_filtered.csv
runs/

# Local test configs (contain machine-specific env/paths)
pipeline_test_config.json
pipeline_test_bertopic_config.json

# Large model / data artifacts — too big for git (track via LFS or keep local)
Models/
omniparser/captions_translated.csv
