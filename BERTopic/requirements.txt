# Pinned versions for the BERTopic env, captured from the working `bertopic_env`.
# Verified known-good: this stack loads Models/bertopic_model_reduced_600 with NO
# scikit-learn version-mismatch warning, and runs the bertopic / fill_category /
# count_embeddings stages end to end.
#
# torch + torchvision are intentionally NOT pinned here, setup_env.sh installs the
# platform-appropriate build (macOS MPS/CPU or Linux CUDA). The working env had
# torch==2.13.0 / torchvision==0.28.0 if you ever need to match those too.
bertopic==0.17.4
sentence-transformers==5.6.1
transformers==5.14.1
tokenizers==0.22.2
scikit-learn==1.7.2
umap-learn==0.5.12
hdbscan==0.8.44
numba==0.66.0
llvmlite==0.48.0
numpy==2.2.6
scipy==1.15.3
plotly==6.9.0
pandas==2.3.3
